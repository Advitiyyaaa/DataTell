# Core data
pandas>=2.0.0
numpy>=1.26.0

# Environment variables
python-dotenv>=1.0.0

# LLM API (Phase 2 & 3)
google-genai>=1.0.0

# Vector store (Phase 3) - includes lightweight ONNX runtime
chromadb>=0.5.0

# BM25 hybrid search (Phase 3)
rank-bm25>=0.2.2

# Agent framework (Phase 4 & 5)
langgraph>=0.2.0
langchain-core>=0.2.0

# Table formatting for synthesizer
tabulate>=0.9.0

# Web API & UI Backend (Phase 6)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
plotly>=5.22.0

# Cloud deploy (Phase 8)
requests>=2.31.0         # Turso Cloud HTTP API client (via turso_client.py)
